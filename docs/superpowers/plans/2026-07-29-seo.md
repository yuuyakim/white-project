# White Project HP SEO対策 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 公開済みの White Project 紹介サイトに、サイトマップ・OGP画像・構造化データ・地域キーワードを入れ、ローカル検索とSNSシェアで拾われる状態にする。

**Architecture:** 既存の Astro 静的サイトへの追加のみ。サイトマップは公式インテグレーションに任せる。OGP画像は既存ブランド素材を `sharp` で合成するワンショットスクリプトで作り、生成物をコミットする（ビルド時生成にしない）。構造化データは `BaseLayout.astro` のベタ書きをやめ、`src/lib/structuredData.ts` に生成関数として切り出し、`BaseLayout` は受け取った配列を出力するだけにする。

**Tech Stack:** Astro 7.1.4、TypeScript（strict）、`@astrojs/sitemap` 3.7.3、`sharp` 0.35.3（devDependency、ビルドには関与しない）

## Global Constraints

- 仕様の正は `docs/superpowers/specs/2026-07-29-seo-design.md`。着手前に必ず読むこと
- 作業ディレクトリは `C:\Users\yuuya\test_prj\kakigori-site`
- **自動テストは書かない。** このリポジトリにはテストランナーが無く、既存の実装計画（`2026-07-28-kakigori-site.md`）でも「自動テストは書かない」方針が明示されている。**検証は毎タスク `npm run build` の成功、`npm run check` の `0 errors`、および `dist/` の出力に対する具体的な確認コマンドで行う。** 各タスクに実行すべきコマンドと期待出力を書いてある
- **表示速度・Core Web Vitals はスコープ外。** Google フォントのレンダーブロッキングには触らない
- **虚偽・不完全なデータを構造化データに出力しない。** 値が未確定のフィールドは出力ごと省く
- **`IceCreamShop` に `address` を直接持たせない。** 会場の住所は間借り先（他店）の住所であるため、各会場を `Place` として `location` に配置する
- Astro 7 では `<script>` の自己終了タグ（`<script ... />`）が**ビルドエラーになる**。必ず `<script ...></script>` と閉じる
- Astro 7 は `compressHTML: 'jsx'` が既定で、式と式の間の改行由来の空白が落ちる。空白に意味がある箇所は1行に収めるか `{' '}` を使う
- 既存のカラートークン5色以外を使わない。今回はCSSをほぼ触らないが、`docs/seo-setup.md` 等で色指定を足さないこと
- コミットメッセージは日本語、Conventional Commits の prefix 付き
- コミットの末尾に `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` を付ける

## ブランチ構成

`main` → `feature/seo` → 作業ブランチ3本。**作業ブランチのPRは base を `feature/seo` にする。** `feature/seo` → `main` のPRだけユーザーのマージを待つ。

| 作業ブランチ | 含むタスク |
|---|---|
| `feat/sitemap-robots` | Task 1 |
| `feat/ogp-image` | Task 2, Task 3 |
| `feat/structured-data` | Task 4, Task 5, Task 6, Task 7 |

各タスク末尾でコミットする。作業ブランチ内の全タスクが終わったら、`/pre-pr-review` を走らせてから PR を作る。

---

## File Structure

| ファイル | 状態 | 責務 |
|---|---|---|
| `astro.config.mjs` | 変更 | `@astrojs/sitemap` の登録 |
| `public/robots.txt` | 変更 | `Sitemap:` 行の追加 |
| `public/ogp.png` | 新規（生成物をコミット） | OGPカード画像 1200×630 |
| `public/ogp.svg` | 削除 | SVGはOGPとして描画されないため |
| `scripts/build-ogp.mjs` | 新規 | `ogp.png` を生成するワンショットスクリプト |
| `src/data/shop.ts` | 変更 | 構造化データ用フィールドの追加（住所・価格帯・所有権確認タグ・トップtitleの接尾辞） |
| `src/lib/structuredData.ts` | 新規 | JSON-LD の生成関数3種。`BaseLayout` から切り出す |
| `src/lib/excerpt.ts` | 新規 | Markdown本文から description 用の抜粋を作る |
| `src/layouts/BaseLayout.astro` | 変更 | メタタグ補強、Props を `jsonLd?: object[]` に変更、JSON-LD生成の追い出し |
| `src/layouts/NewsLayout.astro` | 変更 | `og:type=article`、抜粋description、記事の構造化データ |
| `src/pages/*.astro` | 変更 | 各ページで JSON-LD を組み立てて渡す、description の地域キーワード |
| `docs/seo-setup.md` | 新規 | Search Console / GBP など、コード外の運用手順 |

---

## Task 1: サイトマップと robots.txt

**ブランチ:** `feat/sitemap-robots`（`feature/seo` から切る）

**Files:**
- Modify: `astro.config.mjs`
- Modify: `public/robots.txt`
- Modify: `src/data/shop.ts`
- Modify: `src/layouts/BaseLayout.astro`
- Modify: `package.json`（依存追加）

**Interfaces:**
- Consumes: なし（最初のタスク）
- Produces: `shop.googleSiteVerification: string`（空文字なら meta タグを出力しない）

- [ ] **Step 1: ブランチを作る**

```bash
cd C:/Users/yuuya/test_prj/kakigori-site
git checkout main
git checkout -b feature/seo
git checkout -b feat/sitemap-robots
git branch --show-current
```

期待: `feat/sitemap-robots`

- [ ] **Step 2: 現状にサイトマップが無いことを確認する**

```bash
npm run build
ls dist/sitemap* 2>/dev/null || echo "NO SITEMAP (期待どおり)"
```

期待: `NO SITEMAP (期待どおり)`

- [ ] **Step 3: `@astrojs/sitemap` を入れる**

```bash
npm install @astrojs/sitemap@^3.7.3
```

このパッケージは `peerDependencies` を宣言していないため Astro 7 でもインストールは通る。**動作確認は Step 5 のビルドで行う。** ここでエラーが出た場合は握りつぶさず報告すること。

- [ ] **Step 4: `astro.config.mjs` にインテグレーションを登録する**

`astro.config.mjs` を丸ごと以下にする。

```js
import { defineConfig } from 'astro/config';
import sitemap from '@astrojs/sitemap';

export default defineConfig({
  // 本番の公開URL。canonicalとOGPの絶対URL生成、およびサイトマップのURL生成に使う。
  // workers.dev のサブドメインは無効化してあるため、カスタムドメインが唯一の入口。
  site: 'https://white-project.yuuyakim.com',
  integrations: [sitemap()],
});
```

- [ ] **Step 5: ビルドしてサイトマップが出ることを確認する**

```bash
npm run build
ls dist/sitemap-index.xml dist/sitemap-0.xml
grep -o '<loc>[^<]*</loc>' dist/sitemap-0.xml
```

期待: 2ファイルとも存在し、`<loc>` が6件出る。

```
<loc>https://white-project.yuuyakim.com/</loc>
<loc>https://white-project.yuuyakim.com/contact/</loc>
<loc>https://white-project.yuuyakim.com/menu/</loc>
<loc>https://white-project.yuuyakim.com/news/</loc>
<loc>https://white-project.yuuyakim.com/news/open/</loc>
<loc>https://white-project.yuuyakim.com/venues/</loc>
```

**`404` が含まれていないことを必ず目視で確認する。** 含まれていたら `sitemap({ filter: (page) => !page.endsWith('/404/') })` を足す。

- [ ] **Step 6: `robots.txt` にサイトマップを書く**

`public/robots.txt` を丸ごと以下にする。

```
User-agent: *
Allow: /

Sitemap: https://white-project.yuuyakim.com/sitemap-index.xml
```

- [ ] **Step 7: Search Console 所有権確認タグの器を作る**

`src/data/shop.ts` の `shop` オブジェクトに、`sns` の**直前**に以下を追加する。

```ts
  /**
   * Search Console の所有権確認を meta タグ方式で行う場合のみ、発行された content 値を入れる。
   * DNS TXT レコード方式で確認する場合は空文字のままにする（空ならタグを出力しない）。
   */
  googleSiteVerification: '',
```

- [ ] **Step 8: `BaseLayout` から所有権確認タグを条件出力する**

`src/layouts/BaseLayout.astro` の `<link rel="canonical" ... />` の**次の行**（`{noindex && ...}` の直前）に追加する。

```astro
    {
      shop.googleSiteVerification && (
        <meta name="google-site-verification" content={shop.googleSiteVerification} />
      )
    }
```

`shop` は既に同ファイルで import 済みなので import の追加は不要。

- [ ] **Step 9: ビルドと型チェックを通す**

```bash
npm run build
npm run check
```

期待: ビルド成功、`0 errors`。

- [ ] **Step 10: 所有権確認タグが出ていないことを確認する**

```bash
grep -c 'google-site-verification' dist/index.html || echo "0件（期待どおり）"
```

期待: `0件（期待どおり）`。`googleSiteVerification` が空文字なので出力されない。

- [ ] **Step 11: コミット**

```bash
git add astro.config.mjs public/robots.txt src/data/shop.ts src/layouts/BaseLayout.astro package.json package-lock.json
git commit -m "$(cat <<'EOF'
feat: サイトマップを生成しrobots.txtから参照する

@astrojs/sitemap で /sitemap-index.xml を生成し、robots.txt に
Sitemap 行を追加した。あわせて Search Console の所有権確認を
meta タグ方式でも行えるよう shop.googleSiteVerification を用意した
（既定は空文字で、その場合タグは出力しない）。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 12: PRを出してマージする**

`/pre-pr-review` を走らせ、指摘に対応してから PR を作る。**base は `feature/seo`。** CIが緑なら自分でマージしてよい。

---

## Task 2: OGP画像の生成

**ブランチ:** `feat/ogp-image`（`feature/seo` から切る）

**Files:**
- Create: `scripts/build-ogp.mjs`
- Create: `public/ogp.png`（スクリプトの生成物。**コミットする**）
- Modify: `package.json`

**Interfaces:**
- Consumes: `src/assets/images/brand/logo-on-blue.png`（870×1022、既にリポジトリにある）
- Produces: `public/ogp.png`（1200×630）。Task 3 の `og:image` が参照する

**なぜ既存の `ogp.svg` を変換しないか:** `ogp.svg` は `<text>` で「かき氷専門店」という日本語を描いている。SVG→PNG 変換では実行環境に日本語フォントが要り、無ければ豆腐（□□□）になる。**テキスト描画を発生させない**ため、ロゴ画像の合成で作る。

- [ ] **Step 1: ブランチを作る**

```bash
git checkout feature/seo
git pull --ff-only 2>/dev/null || true
git checkout -b feat/ogp-image
git branch --show-current
```

期待: `feat/ogp-image`

- [ ] **Step 2: `sharp` を devDependency に入れる**

```bash
npm install --save-dev sharp@^0.35.3
```

`sharp` は **devDependency である**こと。生成物をコミットするのでビルドには不要で、CIやCloudflare側にプラットフォーム依存のバイナリを持ち込みたくない。

- [ ] **Step 3: 生成スクリプトを書く**

`scripts/build-ogp.mjs` を新規作成する。

```js
// OGPカード画像を生成するワンショットスクリプト。`npm run ogp` で実行する。
// ブランド素材が変わったときだけ再実行すればよく、ビルドのたびには走らせない。
// 生成物 public/ogp.png はリポジトリにコミットする。
import sharp from 'sharp';
import { fileURLToPath } from 'node:url';

const LOGO = fileURLToPath(new URL('../src/assets/images/brand/logo-on-blue.png', import.meta.url));
const OUT = fileURLToPath(new URL('../public/ogp.png', import.meta.url));

/** OGPの推奨サイズ。X・Facebook・LINE のいずれもこの比率で切られる。 */
const WIDTH = 1200;
const HEIGHT = 630;
/** ロゴの高さ。上下に余白を残すためキャンバス高より小さくする。 */
const LOGO_HEIGHT = 520;

// 背景色はロゴ画像の角のピクセルから取る。ブランド定義値 #5ABDDD を直接使うと
// ロゴ画像側の青とわずかにズレて、合成の継ぎ目が見えることがあるため。
const { data: corner } = await sharp(LOGO)
  .extract({ left: 0, top: 0, width: 1, height: 1 })
  .raw()
  .toBuffer({ resolveWithObject: true });
const [r, g, b] = corner;

const logo = await sharp(LOGO).resize({ height: LOGO_HEIGHT }).toBuffer();

await sharp({
  create: { width: WIDTH, height: HEIGHT, channels: 4, background: { r, g, b, alpha: 1 } },
})
  .composite([{ input: logo, gravity: 'center' }])
  .png()
  .toFile(OUT);

console.log(`public/ogp.png を生成しました: ${WIDTH}x${HEIGHT} / 背景 rgb(${r}, ${g}, ${b})`);
```

- [ ] **Step 4: `package.json` に scripts を足す**

`"scripts"` の `"check"` の次に追加する。

```json
    "ogp": "node scripts/build-ogp.mjs"
```

`"check": "astro check"` の行末にカンマを足すのを忘れないこと。

- [ ] **Step 5: 生成する**

```bash
npm run ogp
```

期待: `public/ogp.png を生成しました: 1200x630 / 背景 rgb(...)` が出る。

- [ ] **Step 6: 寸法を機械的に確認する**

```bash
node -e "const b=require('fs').readFileSync('public/ogp.png');console.log(b.readUInt32BE(16)+'x'+b.readUInt32BE(20))"
```

期待: `1200x630`

- [ ] **Step 7: 目視で確認する**

`public/ogp.png` を開き、以下を確認する。

- ロゴが上下左右で欠けていない
- 合成した背景とロゴ画像の背景に継ぎ目（色の段差）が見えない
- ロゴが極端に小さくない・大きすぎない

継ぎ目が見える場合は、ロゴ画像の四隅の色が一様でない（グラデーション等）ということなので、`LOGO_HEIGHT` を `HEIGHT`（630）に変えて背景を完全に覆う方式に切り替える。**その場合はスクリプトのコメントに理由を残すこと。**

- [ ] **Step 8: コミット**

`public/ogp.png` は生成物だがコミットする（`.gitignore` には入れない）。

```bash
git add scripts/build-ogp.mjs public/ogp.png package.json package-lock.json
git commit -m "$(cat <<'EOF'
feat: OGPカード画像をPNGで生成する

既存の ogp.svg は SVG のため X・Facebook・LINE のいずれもカード画像として
描画しない。ブランド素材 logo-on-blue.png を 1200x630 に合成した
public/ogp.png を生成するスクリプトを追加した。日本語のテキスト描画を
発生させないため、SVGの変換ではなく画像合成で作っている。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: OGPメタタグの補強

**ブランチ:** `feat/ogp-image`（Task 2 の続き。同じブランチ）

**Files:**
- Modify: `src/layouts/BaseLayout.astro`
- Modify: `src/layouts/NewsLayout.astro`
- Delete: `public/ogp.svg`

**Interfaces:**
- Consumes: `public/ogp.png`（Task 2）
- Produces: `BaseLayout` の Props に `ogType?: 'website' | 'article'`（既定 `'website'`）。Task 5 が記事側で使う

- [ ] **Step 1: 現状のOGP出力を記録する**

```bash
npm run build
grep -o '<meta property="og:[^>]*>' dist/index.html
```

期待: `og:type` `og:title` `og:description` `og:url` `og:image`（`ogp.svg`）`og:site_name` の6件。`og:locale` と `og:image:width` は**無い**。

- [ ] **Step 2: `BaseLayout` の Props に `ogType` を足し、既定画像をPNGにする**

`src/layouts/BaseLayout.astro` の `type Props` に追加する。

```ts
  /** お知らせ記事など、記事として扱うページで 'article' にする */
  ogType?: 'website' | 'article';
```

分割代入も更新する。`ogImage` の既定値を `'/ogp.svg'` から `'/ogp.png'` に変える。

```ts
const {
  title,
  description = shop.description,
  ogImage = '/ogp.png',
  wide = false,
  structuredData = false,
  noindex = false,
  ogType = 'website',
} = Astro.props;
```

- [ ] **Step 3: メタタグを差し替える**

`src/layouts/BaseLayout.astro` の `<meta property="og:type" ... />` から `<meta name="twitter:card" ... />` までの**7行を丸ごと**以下に置き換える。

```astro
    <meta property="og:type" content={ogType} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical.href} />
    <meta property="og:image" content={ogImageUrl.href} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content={`${shop.nameJa}のロゴ`} />
    <meta property="og:site_name" content={shop.nameJa} />
    <meta property="og:locale" content="ja_JP" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:image" content={ogImageUrl.href} />
```

`og:image:width` / `og:image:height` は Task 2 で作った画像の実寸に固定している。**`ogImage` を差し替えるページを将来足すときは、この2行が嘘にならないか確認すること。**

- [ ] **Step 4: お知らせ記事を `article` にする**

`src/layouts/NewsLayout.astro` の `<BaseLayout ...>` 開始タグを以下にする。

```astro
<BaseLayout title={title} description={`${formatDate(date)}のお知らせ`} ogType="article">
```

（description は Task 5 で抜粋に差し替える。ここでは `ogType` だけ足す。）

- [ ] **Step 5: `ogp.svg` を消す**

```bash
git rm public/ogp.svg
```

参照元が無くなったため。

- [ ] **Step 6: ビルドと型チェック**

```bash
npm run build
npm run check
```

期待: ビルド成功、`0 errors`。

- [ ] **Step 7: 出力を確認する**

```bash
grep -o '<meta property="og:[^>]*>' dist/index.html
grep -o '<meta name="twitter:[^>]*>' dist/index.html
grep -o 'og:type[^>]*' dist/news/open/index.html
ls dist/ogp.png
ls dist/ogp.svg 2>/dev/null && echo "!! ogp.svg が残っている" || echo "ogp.svg なし（期待どおり）"
```

期待:
- トップの `og:image` が `https://white-project.yuuyakim.com/ogp.png`
- `og:image:width` `og:image:height` `og:image:alt` `og:locale` が出ている
- `twitter:image` が出ている
- 記事ページの `og:type` が `article`
- `dist/ogp.png` が存在し、`dist/ogp.svg` は無い

- [ ] **Step 8: コミット**

```bash
git add src/layouts/BaseLayout.astro src/layouts/NewsLayout.astro public/ogp.svg
git commit -m "$(cat <<'EOF'
feat: OGPメタタグを補強しPNG画像を参照する

og:image を ogp.png に切り替え、width/height/alt と og:locale、
twitter:image を追加した。お知らせ記事は og:type を article にできるよう
BaseLayout に ogType プロパティを足した。参照元が無くなった ogp.svg は削除。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 9: PRを出してマージする**

`/pre-pr-review` を走らせ、指摘に対応してから PR を作る。**base は `feature/seo`。** CIが緑なら自分でマージしてよい。

---

## Task 4: 構造化データの切り出しと店舗スキーマ

**ブランチ:** `feat/structured-data`（`feature/seo` から切る）

**Files:**
- Create: `src/lib/structuredData.ts`
- Modify: `src/data/shop.ts`
- Modify: `src/layouts/BaseLayout.astro`
- Modify: `src/pages/index.astro`
- Modify: `src/pages/venues.astro`

**Interfaces:**
- Consumes: `shop`、`Venue`（`src/data/shop.ts`）
- Produces:
  - `shopJsonLd(site: URL | undefined): object`
  - `breadcrumbJsonLd(items: BreadcrumbItem[], site: URL | undefined): object`（Task 6 で使う）
  - `articleJsonLd(input: ArticleInput, site: URL | undefined): object`（Task 5 で使う）
  - `type BreadcrumbItem = { name: string; path: string }`
  - `BaseLayout` の Props: `structuredData?: boolean` を**削除**し `jsonLd?: object[]`（既定 `[]`）を追加

- [ ] **Step 1: ブランチを作る**

```bash
git checkout feature/seo
git checkout -b feat/structured-data
git branch --show-current
```

期待: `feat/structured-data`

- [ ] **Step 2: 現状の構造化データを記録する**

```bash
npm run build
grep -o 'application/ld+json.*</script>' dist/index.html
grep -c 'application/ld+json' dist/venues/index.html || echo "0件（venuesには無い）"
```

期待: トップに `IceCreamShop` が1件、`venues` には0件。**この出力を控えておき、Step 9 の確認で比較する。**

- [ ] **Step 3: `shop.ts` に構造化データ用フィールドを足す**

`Venue` 型に3フィールドを追加する（`note?: string;` の**直前**）。

```ts
  /** 構造化データ用の郵便番号。ハイフンあり。未確定なら省く */
  postalCode?: string;
  /** 構造化データ用の市区町村。確定しているので必須。例: "名古屋市中区" */
  addressLocality: string;
  /** 構造化データ用の番地。未確定なら省く */
  streetAddress?: string;
```

表示用の `address: string`（例: `'愛知県名古屋市（住所確認中）'`）は**そのまま残す。** 画面表示に使っているため。

次に各会場に `addressLocality` を追加する。**現時点で確定しているのは市までなので `'名古屋市'` を入れる。** 区・番地・郵便番号が店主確認で判明したら、そのとき `addressLocality` を区まで詳しくし `postalCode` / `streetAddress` を足す。

`akawani` の `address` の次の行:

```ts
    addressLocality: '名古屋市',
```

`kakaoc` の `address` の次の行にも同じ行を追加する。

さらに `shop` オブジェクトに2フィールドを追加する。`payments` の**直後**:

```ts
  // TODO: 店主に確認 — 実際の価格帯
  /** 構造化データの priceRange。schema.org は自由文字列を許す */
  priceRange: '¥1,000〜¥1,800',
  /** トップページのtitleに付ける地域キーワード。下層ページは `${title} | ${shop.name}` のまま */
  homeTitleSuffix: '名古屋の間借りかき氷',
```

- [ ] **Step 4: `src/lib/structuredData.ts` を作る**

新規作成する。**このファイルが JSON-LD の唯一の生成元になる。**

```ts
import type { Venue } from '../data/shop';
import { shop } from '../data/shop';

/** 曜日番号（0=日曜）を schema.org の DayOfWeek に対応させる。Venue.days と同じ並び。 */
const DAY_OF_WEEK = [
  'https://schema.org/Sunday',
  'https://schema.org/Monday',
  'https://schema.org/Tuesday',
  'https://schema.org/Wednesday',
  'https://schema.org/Thursday',
  'https://schema.org/Friday',
  'https://schema.org/Saturday',
] as const;

/** 未確定のフィールドを構造化データに出さないための除去。値が空・undefined のキーを落とす。 */
const compact = <T extends Record<string, unknown>>(obj: T): T =>
  Object.fromEntries(Object.entries(obj).filter(([, v]) => v !== undefined && v !== '')) as T;

/**
 * 会場を Place として表す。
 * 間借り先の住所は他店の住所なので、IceCreamShop の address には載せず location に置く。
 * 「（住所確認中）」のような表示用文字列は流し込まず、確定しているフィールドだけを出す。
 */
const venuePlace = (venue: Venue) => ({
  '@type': 'Place',
  name: venue.name,
  address: compact({
    '@type': 'PostalAddress',
    addressCountry: 'JP',
    addressRegion: '愛知県',
    addressLocality: venue.addressLocality,
    postalCode: venue.postalCode,
    streetAddress: venue.streetAddress,
  }),
  openingHoursSpecification: venue.days.map((day) => ({
    '@type': 'OpeningHoursSpecification',
    dayOfWeek: DAY_OF_WEEK[day],
    opens: venue.open,
    closes: venue.close,
  })),
});

/** トップと出店情報ページに出す店舗スキーマ。 */
export const shopJsonLd = (site: URL | undefined) => ({
  '@context': 'https://schema.org',
  '@type': 'IceCreamShop',
  name: shop.nameJa,
  alternateName: shop.name,
  description: shop.description,
  url: new URL('/', site).href,
  image: new URL('/ogp.png', site).href,
  priceRange: shop.priceRange,
  servesCuisine: 'かき氷',
  areaServed: '愛知県名古屋市',
  sameAs: Object.values(shop.sns).filter(Boolean),
  location: shop.venues.map(venuePlace),
});

export type BreadcrumbItem = {
  name: string;
  /** サイトルートからのパス。例: '/menu' */
  path: string;
};

/** パンくず。呼び出し側はトップを含めた全階層を渡す。 */
export const breadcrumbJsonLd = (items: BreadcrumbItem[], site: URL | undefined) => ({
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: items.map((item, index) => ({
    '@type': 'ListItem',
    position: index + 1,
    name: item.name,
    item: new URL(item.path, site).href,
  })),
});

export type ArticleInput = {
  title: string;
  date: Date;
  category: string;
  /** サイトルートからのパス。例: '/news/open' */
  path: string;
  /** description に使う抜粋。作れなかった場合は undefined */
  description?: string;
};

/** お知らせ記事のスキーマ。 */
export const articleJsonLd = (input: ArticleInput, site: URL | undefined) => {
  const url = new URL(input.path, site).href;
  return compact({
    '@context': 'https://schema.org',
    '@type': 'BlogPosting',
    headline: input.title,
    description: input.description,
    datePublished: input.date.toISOString(),
    // 更新日を別途持っていないため公開日と同じ値を入れる
    dateModified: input.date.toISOString(),
    articleSection: input.category,
    mainEntityOfPage: { '@type': 'WebPage', '@id': url },
    url,
    image: new URL('/ogp.png', site).href,
    author: { '@type': 'Organization', name: shop.nameJa },
    publisher: { '@type': 'Organization', name: shop.nameJa },
  });
};
```

- [ ] **Step 5: `BaseLayout` の Props を `jsonLd` に変える**

`src/layouts/BaseLayout.astro` の `type Props` から以下の2行を**削除**する。

```ts
  /** IceCreamShopの構造化データを出すページでのみ true にする（トップページのみ） */
  structuredData?: boolean;
```

代わりに追加する。

```ts
  /** このページで出力する JSON-LD。src/lib/structuredData.ts の生成関数で作る */
  jsonLd?: object[];
```

分割代入の `structuredData = false,` を `jsonLd = [],` に置き換える。

- [ ] **Step 6: `BaseLayout` から JSON-LD のベタ書きを消す**

`const jsonLd = { ... };` の**オブジェクトリテラル全体**（`'@context'` から `sameAs: ...` の閉じ括弧まで）を削除する。ローカル変数名が Props と衝突するため、**消し忘れると型エラーになる。**

`<head>` 内の以下の行を、

```astro
    {structuredData && <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} is:inline></script>}
```

以下に置き換える。

```astro
    {
      jsonLd.map((data) => (
        <script type="application/ld+json" set:html={JSON.stringify(data)} is:inline></script>
      ))
    }
```

**`<script>` を自己終了タグで書かないこと**（Astro 7 ではビルドエラーになる）。

- [ ] **Step 7: トップページで店舗スキーマを渡す**

`src/pages/index.astro` のフロントマターの import に追加する。

```ts
import { shopJsonLd } from '../lib/structuredData';
```

`<BaseLayout title={shop.name} wide structuredData>` を以下にする。

```astro
<BaseLayout title={shop.name} wide jsonLd={[shopJsonLd(Astro.site)]}>
```

- [ ] **Step 8: 出店情報ページでも店舗スキーマを出す**

出店情報は住所・営業時間を最も濃く持つページで、ローカル検索で狙う着地点になるため。

`src/pages/venues.astro` のフロントマターの import に追加する。

```ts
import { shopJsonLd } from '../lib/structuredData';
```

`<BaseLayout` の属性に `jsonLd={[shopJsonLd(Astro.site)]}` を足す。

```astro
<BaseLayout
  title="出店情報"
  description="曜日ごとの出店場所と営業時間のご案内です。名古屋市内の2か所で間借り営業しています。"
  jsonLd={[shopJsonLd(Astro.site)]}
>
```

- [ ] **Step 9: ビルドして構造化データを確認する**

```bash
npm run build
npm run check
node -e "
const fs=require('fs');
for (const p of ['dist/index.html','dist/venues/index.html']) {
  const m=[...fs.readFileSync(p,'utf8').matchAll(/<script type=\"application\/ld\+json\">(.*?)<\/script>/gs)];
  console.log('---',p,m.length,'件---');
  m.forEach(x=>console.log(JSON.stringify(JSON.parse(x[1]),null,2)));
}
"
```

期待:
- `0 errors`
- 両ページに1件ずつ `IceCreamShop`
- `location` に2件の `Place` があり、`赤鰐` は `openingHoursSpecification` が6件（月〜土）、`cafe KAKAOc` は1件（日）
- `address` に `addressRegion: '愛知県'` と `addressLocality: '名古屋市'` があり、**`postalCode` と `streetAddress` のキーが存在しない**
- `IceCreamShop` 直下に `address` キーが**存在しない**
- `image` が `https://white-project.yuuyakim.com/ogp.png`

- [ ] **Step 10: コミット**

```bash
git add src/lib/structuredData.ts src/data/shop.ts src/layouts/BaseLayout.astro src/pages/index.astro src/pages/venues.astro
git commit -m "$(cat <<'EOF'
feat: 構造化データを切り出し店舗スキーマを拡充する

BaseLayout にベタ書きしていた JSON-LD を src/lib/structuredData.ts に移し、
Props を structuredData: boolean から jsonLd: object[] に変更した。
IceCreamShop に image・priceRange・servesCuisine と会場ごとの
openingHoursSpecification を追加。間借り先の住所は他店の住所のため
address には載せず、各会場を Place として location に配置している。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: お知らせ記事のスキーマと抜粋description

**ブランチ:** `feat/structured-data`（Task 4 の続き）

**Files:**
- Create: `src/lib/excerpt.ts`
- Modify: `src/layouts/NewsLayout.astro`

**Interfaces:**
- Consumes: `articleJsonLd`、`ArticleInput`（Task 4）
- Produces: `excerpt(body: string | undefined, max?: number): string | undefined`

- [ ] **Step 1: 現状のdescriptionを確認する**

```bash
grep -o '<meta name="description"[^>]*>' dist/news/open/index.html
```

期待: `content="2026年6月1日のお知らせ"`。日付しか情報が無く、検索結果のスニペットとして機能していない。

- [ ] **Step 2: 抜粋ユーティリティを作る**

`src/lib/excerpt.ts` を新規作成する。

```ts
/**
 * Markdown本文の冒頭から meta description 用の抜粋を作る。
 * 記法を落として一行にし、上限を超えたら句読点で切る。
 * 抜粋が作れない場合は undefined を返し、呼び出し側でフォールバックさせる。
 */
export const excerpt = (body: string | undefined, max = 120): string | undefined => {
  if (!body) return undefined;

  const text = body
    .replace(/^---[\s\S]*?---/, '') // 念のためのフロントマター除去
    .replace(/!\[[^\]]*\]\([^)]*\)/g, '') // 画像は落とす
    .replace(/\[([^\]]*)\]\([^)]*\)/g, '$1') // リンクは表示テキストだけ残す
    .replace(/^[#>\s]+/gm, '') // 見出し・引用の行頭記号
    .replace(/[*_`~]/g, '') // 強調・コード
    .replace(/\s+/g, ' ')
    .trim();

  if (!text) return undefined;
  if (text.length <= max) return text;

  const head = text.slice(0, max);
  const cut = Math.max(head.lastIndexOf('。'), head.lastIndexOf('、'));
  return cut > 0 ? head.slice(0, cut + 1) : `${head}…`;
};
```

- [ ] **Step 3: `NewsLayout` で抜粋と記事スキーマを使う**

`src/layouts/NewsLayout.astro` のフロントマターを丸ごと以下にする。

```ts
import BaseLayout from './BaseLayout.astro';
import type { CollectionEntry } from 'astro:content';
import { formatDate } from '../lib/formatDate';
import { excerpt } from '../lib/excerpt';
import { articleJsonLd } from '../lib/structuredData';

type Props = {
  entry: CollectionEntry<'news'>;
};

const { entry } = Astro.props;
const { title, date, category } = entry.data;

const path = `/news/${entry.id}`;
// 抜粋が作れないときは従来の日付だけの文言にフォールバックする
const description = excerpt(entry.body) ?? `${formatDate(date)}のお知らせ`;
const articleSchema = articleJsonLd({ title, date, category, path, description }, Astro.site);
```

`<BaseLayout ...>` の開始タグを以下にする。

```astro
<BaseLayout title={title} description={description} ogType="article" jsonLd={[articleSchema]}>
```

- [ ] **Step 4: ビルドと型チェック**

```bash
npm run build
npm run check
```

期待: `0 errors`。

`entry.body` の型が `string | undefined` でないというエラーが出た場合は、`excerpt` の引数型を実際の型に合わせる（`undefined` を受け付ける実装のままでよい）。

- [ ] **Step 5: 出力を確認する**

```bash
grep -o '<meta name="description"[^>]*>' dist/news/open/index.html
node -e "
const fs=require('fs');
const m=[...fs.readFileSync('dist/news/open/index.html','utf8').matchAll(/<script type=\"application\/ld\+json\">(.*?)<\/script>/gs)];
m.forEach(x=>console.log(JSON.stringify(JSON.parse(x[1]),null,2)));
"
```

期待:
- description が `6月から、名古屋の2か所を間借りしてかき氷をお出しします。 月曜から土曜は夜の赤鰐で、日曜は昼の cafe KAKAOc です。...` のように本文から作られている（120字以内、句点で終わる）
- `BlogPosting` が1件出ており、`headline` `datePublished` `articleSection`（`その他`）`mainEntityOfPage` `author` `publisher` を持つ

- [ ] **Step 6: コミット**

```bash
git add src/lib/excerpt.ts src/layouts/NewsLayout.astro
git commit -m "$(cat <<'EOF'
feat: お知らせ記事にBlogPostingと本文抜粋のdescriptionを入れる

description が「◯月◯日のお知らせ」で検索スニペットとして機能して
いなかったため、本文冒頭から120字の抜粋を作るようにした。抜粋が
作れない場合は従来の文言にフォールバックする。あわせて BlogPosting の
構造化データを出力する。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: パンくずの構造化データ

**ブランチ:** `feat/structured-data`（Task 5 の続き）

**Files:**
- Modify: `src/pages/menu.astro`
- Modify: `src/pages/venues.astro`
- Modify: `src/pages/contact.astro`
- Modify: `src/pages/news/index.astro`
- Modify: `src/layouts/NewsLayout.astro`

**Interfaces:**
- Consumes: `breadcrumbJsonLd`、`BreadcrumbItem`（Task 4）
- Produces: なし

**注意:** 画面上のパンくずUIは作らない。**構造化データのみ**を出す。Google は視覚的なパンくずが無くても `BreadcrumbList` を検索結果のURL表示に使う。既存デザインは余白で構造を作る方針（`docs/superpowers/specs/2026-07-28-kakigori-site-design.md`）で、パンくずUIの追加は方針に反する。

- [ ] **Step 1: `menu.astro` に足す**

フロントマターの import に追加する。

```ts
import { breadcrumbJsonLd } from '../lib/structuredData';
```

import の下に追加する。

```ts
const breadcrumb = breadcrumbJsonLd(
  [
    { name: 'ホーム', path: '/' },
    { name: 'メニュー', path: '/menu' },
  ],
  Astro.site
);
```

`<BaseLayout` に `jsonLd={[breadcrumb]}` を足す。

- [ ] **Step 2: `venues.astro` に足す**

すでに `shopJsonLd` を import しているので、import 行を以下にする。

```ts
import { breadcrumbJsonLd, shopJsonLd } from '../lib/structuredData';
```

追加する。

```ts
const breadcrumb = breadcrumbJsonLd(
  [
    { name: 'ホーム', path: '/' },
    { name: '出店情報', path: '/venues' },
  ],
  Astro.site
);
```

`jsonLd={[shopJsonLd(Astro.site)]}` を `jsonLd={[shopJsonLd(Astro.site), breadcrumb]}` にする。

- [ ] **Step 3: `contact.astro` に足す**

```ts
import { breadcrumbJsonLd } from '../lib/structuredData';
```

```ts
const breadcrumb = breadcrumbJsonLd(
  [
    { name: 'ホーム', path: '/' },
    { name: 'お問い合わせ', path: '/contact' },
  ],
  Astro.site
);
```

`<BaseLayout` に `jsonLd={[breadcrumb]}` を足す。

- [ ] **Step 4: `news/index.astro` に足す**

**パスが1階層深いので import は `../../`。**

```ts
import { breadcrumbJsonLd } from '../../lib/structuredData';
```

```ts
const breadcrumb = breadcrumbJsonLd(
  [
    { name: 'ホーム', path: '/' },
    { name: 'お知らせ', path: '/news' },
  ],
  Astro.site
);
```

`<BaseLayout` に `jsonLd={[breadcrumb]}` を足す。

- [ ] **Step 5: 記事ページに3階層のパンくずを足す**

`src/layouts/NewsLayout.astro` の import を以下にする。

```ts
import { articleJsonLd, breadcrumbJsonLd } from '../lib/structuredData';
```

`const articleSchema = ...` の下に追加する。

```ts
const breadcrumb = breadcrumbJsonLd(
  [
    { name: 'ホーム', path: '/' },
    { name: 'お知らせ', path: '/news' },
    { name: title, path },
  ],
  Astro.site
);
```

`jsonLd={[articleSchema]}` を `jsonLd={[articleSchema, breadcrumb]}` にする。

- [ ] **Step 6: ビルドと型チェック**

```bash
npm run build
npm run check
```

期待: `0 errors`。

- [ ] **Step 7: 全ページの構造化データを確認する**

```bash
node -e "
const fs=require('fs');
const pages=['dist/index.html','dist/menu/index.html','dist/venues/index.html','dist/contact/index.html','dist/news/index.html','dist/news/open/index.html','dist/404.html'];
for (const p of pages) {
  const m=[...fs.readFileSync(p,'utf8').matchAll(/<script type=\"application\/ld\+json\">(.*?)<\/script>/gs)];
  console.log(p, '->', m.map(x=>JSON.parse(x[1])['@type']).join(', ') || 'なし');
}
"
```

期待:

```
dist/index.html -> IceCreamShop
dist/menu/index.html -> BreadcrumbList
dist/venues/index.html -> IceCreamShop, BreadcrumbList
dist/contact/index.html -> BreadcrumbList
dist/news/index.html -> BreadcrumbList
dist/news/open/index.html -> BlogPosting, BreadcrumbList
dist/404.html -> なし
```

- [ ] **Step 8: コミット**

```bash
git add src/pages/menu.astro src/pages/venues.astro src/pages/contact.astro src/pages/news/index.astro src/layouts/NewsLayout.astro
git commit -m "$(cat <<'EOF'
feat: 下層ページにBreadcrumbListを出力する

検索結果のURL表示にパンくずを使わせるため、トップ以外の全ページに
BreadcrumbList を追加した。画面上のパンくずUIは作らない（余白で構造を
作るデザイン方針に反するため）。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: 地域キーワードと運用手順書

**ブランチ:** `feat/structured-data`（Task 6 の続き）

**Files:**
- Modify: `src/layouts/BaseLayout.astro`
- Modify: `src/pages/index.astro`
- Modify: `src/pages/menu.astro`
- Modify: `src/pages/contact.astro`
- Create: `docs/seo-setup.md`

**Interfaces:**
- Consumes: `shop.homeTitleSuffix`（Task 4）
- Produces: なし

- [ ] **Step 1: 現状のタイトルを確認する**

```bash
grep -o '<title>[^<]*</title>' dist/index.html
```

期待: `<title>かき氷専門店 White Project</title>`。**地域名が一切入っていない。**

- [ ] **Step 2: トップの `<title>` に地域キーワードを入れる**

`src/layouts/BaseLayout.astro` の以下の行を、

```ts
const pageTitle = title === shop.name ? `${shop.nameJa}` : `${title} | ${shop.name}`;
```

以下に置き換える。

```ts
const pageTitle =
  title === shop.name ? `${shop.nameJa} | ${shop.homeTitleSuffix}` : `${title} | ${shop.name}`;
```

区切りは既存に合わせて**半角スペース + `|` + 半角スペース**。全角 `｜` を使わないこと。

- [ ] **Step 3: トップの h1 に地域を入れる**

`src/pages/index.astro` の以下の行を、

```astro
  <h1 class="u-visually-hidden">{shop.nameJa}</h1>
```

以下に置き換える。

```astro
  <h1 class="u-visually-hidden">名古屋のかき氷専門店 White Project</h1>
```

`u-visually-hidden` のままにする（ヒーローのビジュアルを崩さないため）。

- [ ] **Step 4: 各ページの description に地域名を入れる**

`src/pages/menu.astro` の `description` を以下にする。

```astro
<BaseLayout
  title="メニュー"
  description="名古屋のかき氷専門店 White Project のメニューと価格。季節の果実を使った一杯をご紹介します。"
  jsonLd={[breadcrumb]}
>
```

`src/pages/contact.astro` の `description` を以下にする。

```astro
<BaseLayout
  title="お問い合わせ"
  description="名古屋のかき氷専門店 White Project へのお問い合わせ・イベント出店のご依頼はこちらから。"
  jsonLd={[breadcrumb]}
>
```

`venues.astro` と `news/index.astro` の description は**変更しない。** `venues` はすでに「名古屋市内の2か所で間借り営業しています」と地域名を含む。`news` は一覧ページで狙うキーワードが無い。

- [ ] **Step 5: ビルドと型チェック**

```bash
npm run build
npm run check
```

期待: `0 errors`。

- [ ] **Step 6: タイトルとdescriptionを確認する**

```bash
for f in dist/index.html dist/menu/index.html dist/venues/index.html dist/contact/index.html dist/news/index.html; do
  echo "--- $f"
  grep -o '<title>[^<]*</title>' $f
  grep -o '<meta name="description"[^>]*>' $f
done
```

期待: トップが `<title>かき氷専門店 White Project | 名古屋の間借りかき氷</title>`。menu と contact の description に「名古屋」が入っている。

- [ ] **Step 7: 運用手順書を書く**

`docs/seo-setup.md` を新規作成する。

````markdown
# SEO 運用手順

コードでは閉じない、公開後にやる作業をまとめる。実装側の設計は
`superpowers/specs/2026-07-29-seo-design.md` を参照。

## 1. Google Search Console に登録する

サイトが検索でどう扱われているかを見る唯一の窓口。**最初にこれをやる。**

### 推奨: DNS TXT レコード方式

Cloudflare でドメインを管理しているため設定でき、デプロイに依存せず、
レイアウト変更で消える事故もない。

1. https://search.google.com/search-console で「ドメイン」プロパティを選ぶ
2. `yuuyakim.com` を入力する
3. 表示された TXT レコードの値をコピーする
4. Cloudflare ダッシュボード → 該当ドメイン → DNS → レコードを追加
   - タイプ: `TXT` / 名前: `@` / コンテンツ: コピーした値
5. Search Console に戻って「確認」を押す（反映まで数分〜数時間かかることがある）

### 代替: meta タグ方式

DNS を触れない場合に使う。

1. Search Console で「URL プレフィックス」プロパティを選び
   `https://white-project.yuuyakim.com` を入力する
2. 「HTML タグ」の `content="..."` の値をコピーする
3. `src/data/shop.ts` の `googleSiteVerification` にその値を入れる
4. デプロイしてから Search Console で「確認」を押す

## 2. サイトマップを送信する

所有権確認が済んだら、Search Console の「サイトマップ」から以下を送信する。

```
https://white-project.yuuyakim.com/sitemap-index.xml
```

「成功しました」と表示され、検出URL数が 6 になれば正しい。

## 3. 構造化データを検証する

デプロイ後、以下の各URLを https://search.google.com/test/rich-results に通す。
**エラー0 を確認する。** 警告（推奨プロパティの欠落）は、値が未確定なもの
であれば許容してよい。

| URL | 出るべきスキーマ |
|---|---|
| `/` | IceCreamShop |
| `/venues` | IceCreamShop, BreadcrumbList |
| `/menu` | BreadcrumbList |
| `/news/open` | BlogPosting, BreadcrumbList |

## 4. SNSシェアのカード表示を確認する

**実際に貼って確認する。** キャッシュが効くため、修正後は各デバッガで再取得する。

- LINE: トークで自分宛にURLを送る
- X: https://cards-dev.twitter.com/validator
- Facebook: https://developers.facebook.com/tools/debug/

画像が出ない場合、まず `https://white-project.yuuyakim.com/ogp.png` が
直接開けるかを確認する。

## 5. SNSプロフィールにURLを載せる

Instagram（@whiteproject_kakigori）と Threads のプロフィール欄に
サイトURLを入れる。被リンクとしての価値は小さいが、**現状の流入は
ほぼSNS経由**であり、実利用の導線としてここが一番効く。

## 6. Google ビジネスプロフィール（要判断）

マップに店舗として出るための登録。ローカル検索では最も効果が大きい。

**ただし間借り営業では登録が通らない可能性がある。** Google のポリシー上、
他事業者の所在地内で営業する店舗（いわゆる店舗内店舗）は、独自の看板・
独自の営業時間・独自スタッフといった条件を満たす場合に限り登録できる。
White Project は独自の営業時間を持つが、看板や常設の設備の状況によっては
却下されうる。

**また、間借り先（赤鰐 / cafe KAKAOc）の住所で登録することになるため、
事前に間借り先の了承が要る。** 店主の判断が必要な領域なので、こちらの
判断で申請を進めないこと。

申請する場合の注意:

- 住所は実際に営業している会場のもので、かつ了承を得たものだけを使う
- 営業時間は間借り先の営業時間ではなく **White Project の出店時間**を入れる
- サイトの構造化データと登録情報（屋号・営業時間）を食い違わせない

## 7. 効果を見る

Search Console の「検索パフォーマンス」で、以下を月1回ほど見る。

- 「名古屋 かき氷」など地域キーワードでの表示回数
- クリック率が極端に低いページ（title / description の見直し対象）

インデックス登録には数日〜数週間かかる。**登録直後に結果が出なくても
異常ではない。**
````

- [ ] **Step 8: コミット**

```bash
git add src/layouts/BaseLayout.astro src/pages/index.astro src/pages/menu.astro src/pages/contact.astro docs/seo-setup.md
git commit -m "$(cat <<'EOF'
feat: タイトルとdescriptionに地域キーワードを入れ運用手順書を追加

トップの title に地域名が一切入っていなかったため、shop.homeTitleSuffix を
足して「名古屋の間借りかき氷」を付けた。visually-hidden の h1 と menu /
contact の description にも地域名を入れている。あわせて Search Console 登録・
サイトマップ送信・構造化データ検証・ビジネスプロフィールの注意点を
docs/seo-setup.md にまとめた。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 9: PRを出してマージする**

`/pre-pr-review` を走らせ、指摘に対応してから PR を作る。**base は `feature/seo`。** CIが緑なら自分でマージしてよい。

---

## 仕上げ: `feature/seo` → `main`

- [ ] **Step 1: 通しで確認する**

```bash
git checkout feature/seo
npm run build
npm run check
node -e "
const fs=require('fs');
const pages=['dist/index.html','dist/menu/index.html','dist/venues/index.html','dist/contact/index.html','dist/news/index.html','dist/news/open/index.html'];
for (const p of pages) {
  const h=fs.readFileSync(p,'utf8');
  const t=h.match(/<title>([^<]*)<\/title>/)[1];
  const d=h.match(/<meta name=\"description\" content=\"([^\"]*)\"/)[1];
  const ld=[...h.matchAll(/<script type=\"application\/ld\+json\">(.*?)<\/script>/gs)].map(x=>JSON.parse(x[1])['@type']);
  console.log(p);
  console.log('  title:', t);
  console.log('  desc :', d);
  console.log('  ld   :', ld.join(', ') || 'なし');
}
console.log('sitemap:', fs.existsSync('dist/sitemap-index.xml'));
console.log('ogp.png:', fs.existsSync('dist/ogp.png'), '/ ogp.svg:', fs.existsSync('dist/ogp.svg'));
"
```

全ページで title・description・構造化データが意図どおりか、サイトマップと `ogp.png` があり `ogp.svg` が無いことを確認する。

- [ ] **Step 2: PRを作り、`/code-review-lite` を走らせる**

`feature/seo` → `main` の PR を作成する。作成後に `/code-review-lite` を走らせ、指摘は握りつぶさず、直したもの・見送ったものを理由つきで報告する。

- [ ] **Step 3: ユーザーのマージを待つ**

**このPRは自分でマージしない。** `main` は本番に直結するため、ユーザーのマージを待つ。

- [ ] **Step 4: デプロイ後の確認**

マージ・デプロイ後、`docs/seo-setup.md` の「3. 構造化データを検証する」と
「4. SNSシェアのカード表示を確認する」を実行する。**ここまでやって完了。**

---

## 未確定事項（実装中に確認が必要）

以下は店主確認が要る。**揃わない場合は該当フィールドを出力ごと省く**（Task 4 の `compact()` がこれを担保する）。揃った場合は `src/data/shop.ts` を更新するだけでよい。

| 項目 | 現状 | 反映先 |
|---|---|---|
| 各会場の郵便番号・区・番地 | `addressLocality: '名古屋市'` のみ | `Venue.postalCode` / `addressLocality` / `streetAddress` |
| 問い合わせ用メールアドレス | `info@example.com` | `shop.email` |
| 価格帯 | `'¥1,000〜¥1,800'`（仮） | `shop.priceRange` |
| Search Console 登録状況 | 不明 | `docs/seo-setup.md` の手順を実行 |
| GBP 登録の可否 | 不明・店主判断 | `docs/seo-setup.md` の注意を参照 |
